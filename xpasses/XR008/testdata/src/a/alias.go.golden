package a

import (
	"context"
	e "os/exec"
)

func fAlias() {
	failingAlias := // want "avoid os/exec.CommandContext"

		// want "avoid os/exec.CommandContext"

		failingAlias(context.Background(), "true")
}
